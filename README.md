# Soviez Deploy

Canonical GitHub repository for the **Soviez deployment platform** (`Soviez/soviez-deploy`).

## Contents

- `soviez.sh` — supported dual Production/Stage deployment wizard (byte-identical with Soviez ERP copy)
- `src/`, `build/`, `tests/`, `tools/`, `docs/`, `dist/` — modular Soviez.sh platform
- `services/registry-gateway/` — installable private image pull Gateway (`registry.soviez.com`)

## Artifact

See `VERSION` and `dist/soviez.sh` (`0.24.5.3-registry-gateway`).

Commercial Production Release is **NOT** authorized by source publication alone.
