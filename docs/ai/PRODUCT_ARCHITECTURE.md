# Product Architecture (AI)

Compact map — details in `docs/dev/`.

- Dual wizard (ERP≡deploy): host `--init`, Production `--new`
- Modular certified CLI: Stage/update/backup/restore/migration/security/offline
- Security Platform S1–S6
- SaaS: entitlements only; ERP runtime independent
- No `--merge-in`; migration is `--migration-*`

Pointers: `docs/dev/ARCHITECTURE.md`, `docs/dev/COMPONENT_OWNERSHIP.md`.
