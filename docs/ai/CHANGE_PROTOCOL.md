# Change Protocol

```text
understand → inspect owner → explain → classify impact
→ authorize if required → implement focused change
→ owning tests → regression → evidence → governance sync
```

No silent scope expansion. Docs-only missions must not alter `dist/soviez.sh` SHA.
