# Disaster Recovery Posture (S5)

DR-capable requires classified off-host destination (`OFF_HOST_S3_COMPATIBLE` or `OFF_HOST_SFTP`) with integrity/encryption posture. **LOCAL_ONLY ≠ DR-capable** (`SEC_HIGH_BACKUP_LOCAL_ONLY` if falsely claimed). S5 does not authorize S6 or Phase 25.
