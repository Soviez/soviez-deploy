# Security Test Matrix

Canonical TEST-SEC-001..024 map lives in `tests/helpers/s6_cert.sh` and evidence `docs/evidence/security-gate-s6/TEST_SEC_001_024_MATRIX.md`.

Focused S6 matrix: **PASS** (`fail_count=0`, `ids=24`). E2E + adversarial matrices: **PASS**. Nested full execute / `run_all` may still be PENDING at evidence write.
