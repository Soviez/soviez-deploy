# Webmin / Virtualmin (S2)

**Soviez.sh NEVER installs Webmin or Virtualmin.** Supported installers detect and classify exposure only; they do not install, enable, or bundle Webmin/Virtualmin.

Detect + classify exposure. Do not remove or firewall-off blindly. Public :10000 without TLS is FAIL/HIGH; with TLS WARNING pending owner allowlist/VPN decision.
