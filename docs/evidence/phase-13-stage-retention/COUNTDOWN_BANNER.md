# Countdown Banner
The local neutralized Stage receives text and HTML banner artifacts. English line one is `Stage environment · Neutralized · <countdown>`.

Covered phrases: `1 day remaining`, `N days remaining`, `Deletion scheduled today`, and `Deletion overdue — Needs Action`. The banner also reports scheduled date, timezone/extension maximum, or that automatic deletion was blocked to protect data.
