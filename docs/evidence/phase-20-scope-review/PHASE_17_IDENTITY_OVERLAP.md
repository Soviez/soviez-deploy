# PHASE_17_IDENTITY_OVERLAP.md

Reuse: migration pair, bootstrap, mTLS trust, host fingerprints, token **eligibility** (read-only), `assert_no_transfer` on non-20 surfaces, Abort semantics that never burn tokens.

Do not: call `begin_license_migration` from Phase 17 paths; confuse pair eligibility with consume; burn during pair/bootstrap.
