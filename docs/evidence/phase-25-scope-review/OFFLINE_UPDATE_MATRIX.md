# Offline update matrix

```text
issue signed bundle → transfer media → air-gap import → verify → backup → apply → switch → receipt → optional later reconcile
```

Must prove: dual entitlement; no network during apply; OCI digest exact; replay denied; unreconciled ERP independent; rollback path.

Runtime: AIRGAP_REQUIRED + REAL_*.
