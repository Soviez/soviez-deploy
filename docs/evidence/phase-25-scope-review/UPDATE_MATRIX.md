# Update matrix (connected)

```text
current → entitlement → Registry ticket → candidate → backup → validate → switch → post-check → rollback proof
```

Must prove: exact one target; signed artifacts; short-lived Registry creds cleaned; reboot/recovery; support expiry denies update without shutting ERP; interrupted upgrade recovery (P15).

Runtime: REAL_DOCKER + REAL_ODOO + REAL_POSTGRES + REAL_REGISTRY + REAL_NETWORK.
