# Reboot / recovery matrix

Mandatory revalidation (aggregate + at least one live interruption each class):
- Update interrupted / host reboot
- Offline apply reboot (P23 real reboot suite)
- Stage reboot recovery
- Migration transfer resume after interruption
- Ops reattach/recover

Colima default profile: do not delete unless proven disposable; prefer ephemeral cert lifecycle.
