# ODOO_DB_VALIDATION

`soviez_s5_check_odoo_pg` verifies Odoo↔Postgres connectivity after restarts/updates. Inject DB fail → FAIL. Test mode may skip absent containers unless `SOVIEZ_S5_REQUIRE_CONTAINERS=1`. Focused suite: PASS.
