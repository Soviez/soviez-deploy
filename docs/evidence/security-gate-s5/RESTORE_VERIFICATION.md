# RESTORE_VERIFICATION (evidence)

`restore_verify.sh` — restore verification remains fail-closed and composes with S4 quarantine for untrusted sources. S5 adds integrity/posture gates around backup artifacts. Focused suite: PASS.
