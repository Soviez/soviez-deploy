# S1_REGRESSION

S1 containment not weakened. Nested S1 skipped when `SOVIEZ_S5_SKIP_NESTED_REGRESSIONS=1`; prior S1 PASS preserved. Authoritative nested confirmation pending full `run_all` if incomplete.
