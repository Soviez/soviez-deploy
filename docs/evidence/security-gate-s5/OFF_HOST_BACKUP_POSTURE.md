# OFF_HOST_BACKUP_POSTURE

Off-host = customer-controlled S3-compatible or SFTP only — never Soviez SaaS. Disposable MinIO fixture + SFTP classify: **PASS**. Credentials must not appear in backup secret scan evidence.
