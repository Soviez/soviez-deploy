# DNS_VALIDATION

`soviez_s5_check_dns` / Docker DNS helpers: internal target (default `db`) must resolve in stack context; external probe gated by online vs offline/quarantine mode. Inject `SOVIEZ_S5_INJECT_DNS_FAIL=1` → FAIL. Focused suite: PASS.
