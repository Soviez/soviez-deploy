# CLIENT_REGISTRY_CONTRACT

Verified present in public tree / artifact:

| Capability | Status |
|------------|--------|
| Registry base URL (`SOVIEZ_REGISTRY_GATEWAY_URL`) | PASS |
| SaaS Registry ticket request path (`src/api/registry_client.sh`) | PASS |
| Temporary Docker auth (`DOCKER_CONFIG` mktemp) | PASS |
| Exact digest validation | PASS |
| Credential cleanup / assert clean | PASS |
| User-facing errors via `soviez_die` | PASS |
| Offline independence (offline bundle path; ERP without continuous Gateway) | PASS |

CLIENT_REGISTRY_CONTRACT = PASS
