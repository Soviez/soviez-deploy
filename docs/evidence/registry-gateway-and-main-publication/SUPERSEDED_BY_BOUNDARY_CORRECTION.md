# SUPERSEDED — publication policy corrected

This evidence pack documented the **temporary incorrect** publication of the Registry Gateway **server** into `Soviez/soviez-deploy/services/registry-gateway/`.

**Superseded by:** `docs/evidence/public-deploy-boundary-correction/`

**Current policy:** `Soviez/soviez-deploy` is client-side only. Gateway server source is internal (`soviez-registry-gateway/`).

Historical files in this folder are retained as `HISTORICAL_EVIDENCE` and must not be treated as active publication instructions.
