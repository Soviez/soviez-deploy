# Data egress audit

Start payload fields only: public_key, fingerprint, label, protocol_version, nonce (+ HTTPS infra metadata).

Never: ERP business data, dumps, filestore, passwords, activation keys, private keys.

Device auth grants **no** commercial entitlement.
