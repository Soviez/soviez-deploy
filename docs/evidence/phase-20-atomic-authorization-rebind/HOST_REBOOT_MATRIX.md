# HOST_REBOOT_MATRIX

## Design (known)

Phase 20 ops state persisted under `$SOVIEZ_MIG_ROOT/ops/` and authorization dirs.

Reboot recovery should resume from committed receipt + idempotency (no double consume).

## Certification

**Result:** Pending certification run
