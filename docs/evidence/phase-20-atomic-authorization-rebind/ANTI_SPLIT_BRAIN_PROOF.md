# ANTI_SPLIT_BRAIN_PROOF

## Design (known)

`split_brain_validate` requires destination `public_route=false`, no production_domain, source grace present.

Static gates block cutover/DNS env flags.

## Certification

**Result:** Pending certification run


## Certification result

**PASS** — recorded in FINAL_REPORT / TEST_RESULTS (2026-08-03). Installer `0.20.0-phase20`, run_all exit 0.
