# DESTINATION_BACKUP_PROOF

## Design (known)

Post-activation verified backup marker at `$SOVIEZ_MIG_ROOT/activation/{auth_id}/backup/backup.json`.

Phase 21 readiness blocks if missing (`destination_backup_missing`).

Default: `SOVIEZ_MIG_P20_REQUIRE_DEST_BACKUP=1`.

## Certification

**Result:** Pending certification run
