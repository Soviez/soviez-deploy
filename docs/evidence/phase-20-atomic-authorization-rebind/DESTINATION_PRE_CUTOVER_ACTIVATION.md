# DESTINATION_PRE_CUTOVER_ACTIVATION

## Design (known)

Sequence: binding → grace → stage rebind → split-brain validate → health → backup marker.

Output: `activation.json` with `destination_status=production_licensed_pre_cutover`.

## Certification

**Result:** Pending certification run
