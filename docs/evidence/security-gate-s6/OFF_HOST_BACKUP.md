# OFF_HOST_BACKUP

PASS:
- `LOCAL_ONLY` ⇒ `dr_capable=false` (LOCAL_ONLY ≠ DR)
- Off-host MinIO disposable classified S3-compatible
- SFTP classified `OFF_HOST_SFTP`
- Claiming DR on LOCAL_ONLY fails gate
