# Legacy retirement

- Root `soviez.sh` = public bootstrap (modular install)
- Legacy wizard relocated to `legacy/soviez-wizard.sh` (migration glue only)
- `update_self` retired: returns error; no auto-invoke; no unsigned overwrite
