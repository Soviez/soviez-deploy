# PHASE_11_OWNERSHIP_PRESERVATION

Phase 12 modules are additive under `src/ssl/*`, `src/nginx/ownership.sh`, `src/commands/ssl.sh`.
Initial Stage create path (`commands/stage.sh`, `stage/domain_ssl.sh`) unchanged in responsibility.
Self-signed rejection retained and strengthened via structured `SELF_SIGNED_NOT_ALLOWED`.
