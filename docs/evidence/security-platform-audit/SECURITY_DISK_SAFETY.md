# Security disk safety

Scans must not leave: scan containers, temp DBs, extracted backups, networks, volumes, large caches, raw logs unbounded.
Exact-label ephemeral cleanup only — **no global prune**.
