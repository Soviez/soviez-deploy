# S3 destination E2E
S3-compatible profile put/test under `SOVIEZ_TEST_MODE` filesystem fixture (`s3-fixture/` under backup root). Encryption mandatory for kind `s3`.

**Gap:** disposable MinIO (or equivalent) multipart upload/resume e2e not run this certification pass → contributes to Phase 16 **PARTIAL**.
