Generation: Python secrets. Storage: env sheets mode 600.

**Residual:** app DB password still in Odoo container env / docker inspect (not bootstrap). Documented; not redesigned into a secrets platform in S1.
