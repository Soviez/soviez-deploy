# MIGRATION_CLI_CONTRACT

| Item | Expected | Observed |
|------|----------|----------|
| `--merge-in` | NOT_SUPPORTED | Not in parser; docs explicitly forbid |
| Canonical | `--migration-*` | Documented in user/dev migration docs |

Search of operator docs: only negative references ("does not exist" / "not supported").  
Historical evidence may contain old wording — not operator guidance.

**Stale `--merge-in` as supported command: NONE in publishable canonical docs/code.**
