# Models
ir.actions.server, ir.config_parameter, ir.ui.view, ir.cron, base.automation, res.users, res.groups, ir.module.module — optional tables skipped safely; `SOVIEZ_S3_REQUIRE_ODOO_SCHEMA=1` fail-closed.
