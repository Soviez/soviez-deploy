# Reboot recovery
- Relies on Phase 14 canonical sync + update state files under `$SOVIEZ_UPDATE_ROOT/operations`
- `--update-recover` / `--update-reattach` implemented
- Full host-reboot interrupt matrix for every checkpoint: not hardware-exercised this session → PARTIAL factor
