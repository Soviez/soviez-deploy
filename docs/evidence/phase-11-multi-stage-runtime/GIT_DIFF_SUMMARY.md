# GIT_DIFF_SUMMARY

`soviez-sh` has **no commits** on `main` yet; entire tree is untracked/dirty.

Phase 11 adds Stage runtime modules, CLI, tests, docs, evidence, VERSION bump, and regenerates `dist/soviez.sh`.

**No commit / push performed** (authorized constraint).

`soviez-saas` unchanged for Phase 11.
