# Rollback Image Protection

After successful switch, previous digest is recorded and classified `rollback`.
Protected for the 24h safety window (and while rollback manifests reference it).

Code: `IMAGE_CLEANUP_ROLLBACK_IMAGE_PROTECTED`.
PASS — v14 retained while current is v15.
