# Stopped Container Protection

`docker create` then stop leaves a container reference.
Classifier: `used_by_stopped_container` / `IMAGE_CLEANUP_STOPPED_CONTAINER_REFERENCE`.

PASS — stopped `soviez-p15-stopped-ref` protected `p15-v13` until removed.
