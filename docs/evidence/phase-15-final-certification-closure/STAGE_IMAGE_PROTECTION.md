# Stage Image Protection

Stage `identity.json` `image_digest` is scanned as `stage_inventory`.
While a Stage references a digest, classification is `used_by_stage` → not deleted.

PASS in final cert with disposable `stage-final-1`.
