# MERGE_IN_SOURCE_AUDIT

No `--merge-in` in `src/cli/parse.sh`, Soviez ERP, or soviez-deploy.

Phase-17 `LEGACY_MERGE_IN_REVIEW.md`: never existed; `--migration-*` is the greenfield CLI.

Timeline: planned name historically → never implemented → superseded by P17–P22 `--migration-*` → certified without merge-in → documented NOT_SUPPORTED.
