# ROUTING_PLAN_OBJECT

Signed routing readiness object includes pair binding, migration FQDN, landing/TLS summaries, source fingerprint, authorization flags (all cutover/transfer false), expiry 24h.

Show: `--migration-routing-show <plan-id>`
