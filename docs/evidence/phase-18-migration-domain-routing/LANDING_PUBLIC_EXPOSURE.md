# LANDING_PUBLIC_EXPOSURE

Public mig-subdomain exposure is allowed only after DNS ownership + TLS preparation (OD-28).

Landing is temporary; Abort removes destination landing artifacts by default (OD-23).
