# DNS_PROVIDER_MANUAL_PATH

Manual DNS is first-class: `--migration-dns-instructions` prints exact TXT/A records for owner execution.

Automated adapters: stub/mock fixture provider (`mock_fixture`) for tests only; full commercial provider adapters deferred (OD-12/13).

Abort does **not** auto-delete owner DNS (OD-14).
