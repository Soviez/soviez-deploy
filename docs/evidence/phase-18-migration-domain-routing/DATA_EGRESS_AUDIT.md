# DATA_EGRESS_AUDIT

Phase 18 is local-first. Permitted connected metadata (if any future assist call is enabled) limited to pair/domain/challenge status fields — see `docs/ai/DATA_EGRESS_CONTRACT.md`.

**Never egress:** dumps, filestore, TLS private keys, DNS provider secrets, unrestricted logs, business records.
