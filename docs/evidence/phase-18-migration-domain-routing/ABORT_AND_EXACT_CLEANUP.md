# ABORT_AND_EXACT_CLEANUP

`--migration-domain-abort <pair-id>`:

- Removes landing site directory
- Clears challenge/TLS/routing prep state on destination
- Does not enable transfer/cutover
- Unit: landing cleaned on abort
