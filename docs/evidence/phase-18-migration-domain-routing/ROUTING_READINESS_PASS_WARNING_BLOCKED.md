# ROUTING_READINESS_PASS_WARNING_BLOCKED

Unit path produces `ROUTING READINESS — PASS` with banners:

- `NO PRODUCTION CUTOVER`
- `SOURCE DNS UNCHANGED BY SOVIEZ`
- Token not consumed; transfer denied; cutover denied

WARNING/BLOCKED classifications implemented in `routing/readiness.sh` + drift checks.
