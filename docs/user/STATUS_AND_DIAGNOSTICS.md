# Status and Diagnostics

```bash
soviez.sh --operations
soviez.sh --operation-status <id>
soviez.sh --operation-logs <id>
soviez.sh --security-status
soviez.sh --security-report
soviez.sh --ssl-status
soviez.sh --stage-status <id>
soviez.sh --backup-list
soviez.sh --update-status <id>
soviez.sh --migration-status <id>
```

Operations survive terminal disconnect; reattach instead of starting duplicates.
