# Close the rollback window

When stabilization passes, close the rollback window with an explicit confirmation. Automatic rollback is then disabled. The source remains retained for manual recovery. Nothing is deleted.
