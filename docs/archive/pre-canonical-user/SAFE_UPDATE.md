# Safe update

## Planned
Updates require naming exactly one Production system and an active annual support entitlement linked to that system's license.

## Existing (legacy)
`soviez.sh --update` may update all systems and pulls the rolling `latest` image — this will be replaced.
