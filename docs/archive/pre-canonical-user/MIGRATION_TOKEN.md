# Migration Token

## Existing
Purchase migration credits; each successful hardware rebind consumes one token.
Retries after a failed attempt must not consume an extra token once the protocol is hardened in the new engine.
