# Migration stabilization

After cutover, Soviez observes the destination for a stabilization period (default 24 hours) before the rollback window can be closed. Health, backups, DNS, and source write absence are checked across the period—not only once.
