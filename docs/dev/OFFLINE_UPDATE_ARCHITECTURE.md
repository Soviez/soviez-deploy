# Offline Update Architecture

Owners: `src/offline_bundle/`, `src/offline_update/`, `src/offline_trust/`.

Signed bundles, trust packages, import, apply, receipts, reconciliation without stopping ERP. Codes: `src/offline_bundle/codes.sh`.
