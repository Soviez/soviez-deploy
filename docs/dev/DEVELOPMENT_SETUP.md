# Development Setup

- macOS/Linux with Docker (Colima supported in tests)
- `bash build/assemble.sh` when src changes
- `bash tests/run_all.sh` for aggregate
- Never set production secrets in fixtures
