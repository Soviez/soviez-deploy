# Migration Source Archive Protocol

Plan → inventory → DB dump → filestore package → manifests → encrypt → sign → verify → restore test.  
`purge_authorized=false` · `deletion_performed=false` always.  
Minimum: verified archive + independent retained recovery backup.
