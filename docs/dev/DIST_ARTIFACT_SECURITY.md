# Dist Artifact Security

Gate: `soviez_security_scan_dist` / `tools/secret_scan.sh dist`.

Required zeros in production installer:
- service-role credentials
- private keys (PEM)
- embedded Registry credentials
- customer secrets
