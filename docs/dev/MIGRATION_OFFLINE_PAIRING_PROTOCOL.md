# Migration Offline Pairing Protocol

Export/import signed packages for discovery/bootstrap/pair/readiness. Quarantine on import; replay hash store; expiry; no permanent private key export; `data_transfer_authorized=false`.
