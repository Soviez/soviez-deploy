# Migration Readiness Protocol

`sudo soviez.sh --migration-readiness <pair-id>` → PASS | WARNING | BLOCKED.

Checks identity, platform, capacity (+25%), backup classification, token eligibility (never consume), mTLS, Stage plan.

Signed report TTL 24h; invalidate on material input change.
