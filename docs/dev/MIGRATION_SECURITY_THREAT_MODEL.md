# Migration Security Threat Model (Phase 17)

Mitigations: exact IDs, signed objects, bootstrap code replay protection, fingerprint confirmation, mTLS certs, refuse `latest`, static gates against transfer/DNS/token burn/activation.

Malicious Root on either host can always forge local state — out of scope for network/operator mitigations.
