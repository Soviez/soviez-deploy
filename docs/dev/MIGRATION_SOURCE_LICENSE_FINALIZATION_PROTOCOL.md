# Source License Finalization Protocol

Transition → `migrated_source_archived`.  
Deny: Production login, writes, update, Stage create, clone, rebind, second migration/slot.  
Allow: diagnostics, backup metadata, isolated restore-test, support recovery metadata.
