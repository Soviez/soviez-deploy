# Customer-side lifecycle helpers

These services are **customer-host** tooling shipped with Soviez.sh:

- `migration-authorization-ledger/` — SQLite fixture ledger for TEST_MODE / offline authorization certification
- `stage-operation-helper/` — Stage ticket verification helper

Do **not** add Registry Gateway server implementation here.
